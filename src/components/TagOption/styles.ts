import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    gap: 12,
    flexDirection: 'row',
    flexWrap: 'wrap'
  },
  option: {
    flex: 1,
    minWidth: 153,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 8
  }
})